# Core (unified pipeline)
torch>=2.1
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Data / imaging
imagecodecs>=2024.1     # JPEG2000-compressed OME-TIFF (Xenium morphology)
tifffile>=2024.1
remotezip>=0.12         # range-download members from the 10x _outs.zip
h5py>=3.9

# Self-supervised morphology embeddings
# DINOv2 is loaded via torch.hub (facebookresearch/dinov2, dinov2_vits14)

# Optional (legacy single-axis components)
scanpy>=1.10
anndata>=0.10
scvi-tools>=1.1
pot>=0.9                # optimal transport (time-series world model)
